# 계정 정보 (템플릿)

이 파일은 형식 예시입니다. 실제 값은 **`docs/ACCOUNTS.md`** 에 저장하며, 그 파일은 `.gitignore` 에 등록되어 커밋되지 않습니다.

AI는 계정 정보가 필요할 때 먼저 `docs/ACCOUNTS.md` 를 읽고, 항목이 있으면 다시 묻지 않습니다.
사용자가 새 계정 정보를 주면 즉시 여기에 추가하고 되묻지 않습니다.

> 고위험 값(결제, 운영 DB, 배포 키)은 이 파일이 아니라 `.env` / 키체인 / CI 시크릿에 보관하세요.

---

## npm

- registry: https://registry.npmjs.org
- user:
- email:
- token: (`.npmrc` 또는 `NPM_TOKEN` 환경변수 참조)

## GitHub

- user: jeff0410
- email: pazwsx@naver.com
- repo: https://github.com/jeff0410/dak-krds
- token:

## 기타 서비스

| 서비스 | 용도 | 계정 | 비밀번호 / 토큰 | 비고 |
| --- | --- | --- | --- | --- |
| | | | | |
